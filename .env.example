OPENFRAME_PORT=3100
# Use 127.0.0.1 when a VPN/HTTPS proxy runs directly on the Docker host.
OPENFRAME_BIND_ADDRESS=0.0.0.0
# Set both values when placing the server behind an HTTPS reverse proxy.
PUBLIC_URL=
COOKIE_SECURE=false
